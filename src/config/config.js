const config = {
  // server:
  //   !process.env.NODE_ENV || process.env.NODE_ENV === "development"
  //     ? "http://localhost:8080"
  //     : "https://evening-brook-56461.herokuapp.com",
  server: 'https://evening-brook-56461.herokuapp.com',
};

export default config;
